## Features
- Dual-mode focus timer (Pomodoro style)
- Task manager with persistent storage
- Built-in calculator
- User login & secure data tracking (Node.js, Express, MySQL)
## Setup
1. Clone the repo
2. Run `npm install`
3. Add your MySQL connection details
4. Run `node flowcheckserver.js`
